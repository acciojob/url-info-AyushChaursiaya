//your JS code here. If required.
const currentURL = window.location.href;
const currentLength = currentURL.length;

// const urlLenght = prompt('The length of the URL is:', currentURL);
alert(currentLength);